<manifest xmlns:android="http://schemas.android.com/apk/res/android"

    package="br.com.stant.libraries.cardshowviewtakenpicturesview">

    <uses-permission android:name="android.permission.READ_PHONE_STATE" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.VIBRATE"/>
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <uses-feature android:name="android.hardware.camera" />

    <application
        android:allowBackup="true"
        android:supportsRtl="true">

        <activity
            android:name=".camera.CameraActivity"
            android:configChanges="screenSize|orientation|screenLayout|navigation"
            android:theme="@style/AppThemeCamera" />

        <activity
            android:theme="@style/Theme.MaterialComponents.Light.NoActionBar"
            android:name="br.com.stant.libraries.cameraimagegalleryview.activities.CardImageGalleryView"/>
        <activity android:theme="@style/Theme.MaterialComponents.Light.NoActionBar"
            android:name="br.com.stant.libraries.cameraimagegalleryview.activities.FullScreenImage"/>


        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.provider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/provider_paths"/>
        </provider>

    </application>

</manifest>
